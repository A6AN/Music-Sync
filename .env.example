# Generate a secure secret key:
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-generate-a-new-one

# Database (use PostgreSQL in production, SQLite for development)
DATABASE_URL=sqlite:///music_sync.db
# DATABASE_URL=postgresql://user:password@localhost:5432/music_sync

# Flask configuration
FLASK_ENV=development
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# PostgreSQL password for Docker
DB_PASSWORD=your-secure-db-password-here

# Optional: Spotify Client ID for OAuth (if implementing full OAuth flow)
# SPOTIFY_CLIENT_ID=your-spotify-client-id
# SPOTIFY_CLIENT_SECRET=your-spotify-client-secret
